# Denmark CVR distribution API identification.
# Erhvervsstyrelsen requires every request to identify the caller via
# User-Agent. Register your app name + contact email once by email, then
# fill these in. No password / basic auth is needed.
CVR_APP_NAME=NordicMarketResearch
CVR_CONTACT_EMAIL=you@example.com
